# ---------------------------------------------------------------------------
# Secrets — never commit these.
# Tutorials 3 and 4 ask you to create a .env holding GROQ_API_KEY next to the
# notebooks. That is the repo root, so it must be ignored here.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.key
secrets.yaml

# ---------------------------------------------------------------------------
# Python environments and caches
# ---------------------------------------------------------------------------
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/

# ---------------------------------------------------------------------------
# Notebook scratch
# ---------------------------------------------------------------------------
.ipynb_checkpoints/
*-checkpoint.ipynb

# ---------------------------------------------------------------------------
# Generated by the tutorials — all reproducible, none worth versioning
# ---------------------------------------------------------------------------
# tutorial 3: Chroma vector store (persist_directory="./db")
db/
chroma.sqlite3
*.sqlite3
*.db

# tutorial 5: Trainer checkpoints and the saved model
sentiment-distilbert/
runs/
wandb/

# tutorial 1: tokenizers saved with save_pretrained
my-tokenizer/

# bonus notebook (multimodal RAG): the PDF it downloads and slices
attention_full.pdf
attention_p1-9.pdf

# model/dataset caches, if HF_HOME is pointed inside the repo
.cache/
hf_cache/

# ---------------------------------------------------------------------------
# Existing course material kept out of version control
# ---------------------------------------------------------------------------
llm agents/

# ---------------------------------------------------------------------------
# OS noise
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
